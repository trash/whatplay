{
    "dependencies": {
        "typescript": "3.7.4"
    },
    "scripts": {
        "clean": "cd client && npm run clean && cd ../server && npm run clean",
        "build": "cd server && npm run build && cd ../client && npm run build",
        "start": "cd serve && npm run start",
        "test": "jest"
    },
    "repository": {
        "type": "git",
        "url": "git@github.com:trash/whatplay.git"
    },
    "license": "MIT",
    "devDependencies": {
        "prettier": "npm:@brigad/prettier"
    }
}
